apiVersion: v1
kind: ConfigMap
metadata:
  name: bankbot-api-config
data:
  APP_ENV: production
  LOG_LEVEL: INFO
  API_CORS_ORIGINS: https://chat.example.com
  QDRANT_COLLECTION: vietcombank_public_docs
  LLM_PROVIDER: openai
  EMBEDDING_PROVIDER: openai
  RERANKER_PROVIDER: local
